framework:
    test: true
    secret: 'test'

lingoda_ai:
    providers:
        gemini:
            api_key: 'test-gemini-key'
            default_model: 'gemini-3.1-flash-lite'
    logging:
        enabled: false
    rate_limiting:
        enabled: false  # Provider client is registered as an alias of the base client in this mode
